---
name: Publish Site

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: https://git.zyria.de/pyrox/actions/hugo@v1
        with:
          ZYRIA_TOKEN: ${{ secrets.ZYRIA_TOKEN }}
