---
name: Publish Site

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: https://git.zyria.de/pyrox/actions/hugo@a38344044e10f21e926de8211be061f5e384db4d # v1
        with:
          ZYRIA_TOKEN: ${{ secrets.ZYRIA_TOKEN }}
