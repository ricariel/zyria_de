---
name: Publish Site

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: https://git.zyria.de/pyrox/actions/hugo@main
        with:
          ZYRIA_TOKEN: ${{ secrets.ZYRIA_TOKEN }}
